# 二开功能清单

本文档只记录本项目相对官方 `dujiao-next` 的定制功能。每次同步上游或新增、删除二开功能后必须更新，用于区分“二开回归”与“上游已有问题”。

## 当前保留的二开

### 游客手机号与订单密码

- 游客下单必须填写手机号和订单密码，邮箱可选。
- 游客查单、订单详情和支付链路使用手机号与订单密码完成身份校验。
- 收货订单默认可将收件手机号同步为游客查单手机号。

主要范围：用户端结算/游客查单、订单 API DTO 与校验、游客订单鉴权。

### 收货地址

- 商品可要求填写收货地址。
- 支持省、市、区、街道四级地区选择。
- 游客可在本地保存并复用最近一次收货地址，也可重填、清空表单或删除记录。

主要范围：地址区划模块、订单收货地址字段与校验、用户端结算页和地址选择组件。

### VPay

- 支持 VPay 支付渠道配置、发起支付和异步回调。
- VPay 已适配当前支付模块和统一路由架构。

主要范围：支付 provider、支付回调、管理端支付渠道配置。

### CAP 验证器

- 验证器名称固定为“CAP 验证器”。
- 支持后台安全设置、公开配置、前端验证组件和游客下单验证。
- CAP 已接入当前统一 captcha 模块，不保留旧版独立验证码服务。

主要范围：captcha 模块、安全设置 schema、管理端安全设置、用户端验证码组件与认证/结算场景。

### 模板与样式兼容

- 保留 classic/default 与 vault 模板兼容。
- 保留二开表单、弹窗、地址选择和结算控件所需的主题样式工具类。
- 新页面优先复用现有主题变量和组件，不重复新增一套样式体系。

### 移动端分步结算

- 移动端结算按商品、收货、购买信息、优惠券、支付方式分步引导。
- 支持步骤确认、修改后重新确认、错误定位和底部固定提交栏。
- 订单预览、库存、地址、游客信息、CAP、钱包、支付渠道和最终提交统一复用新版 `useCheckout`，移动适配层只负责界面步骤状态。

主要范围：`frontend/user/src/components/checkout/mobile`、`useMobileCheckoutFlow.ts`、`useMobileCheckoutAdapter.ts` 和 `Checkout.vue`。

## 已由上游接管

### 首页公告弹窗

- 官方已经提供公告弹窗，本 fork 不再维护旧 `is_home_popup` 实现。
- 同步上游时应保留官方公告模型和展示逻辑，不恢复旧版二开路径。

## 不属于本项目二开

- SKU 批发价、会员价。
- 分销、站点定制及相关 reseller 功能。
- 上述功能按官方实现处理；除非二开适配直接破坏它们，否则不作为 fork 回归问题修复。

## 上游同步审查边界

- 只修复因二开与上游合并、迁移或适配导致的错误。
- 不修复上游原本存在且本 fork 未改动的错误。
- 二开与上游新功能发生产品冲突时，先说明差异和影响，由维护者决定保留哪一方。
